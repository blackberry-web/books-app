import { useSearchParams, usePathname } from "next/navigation";

const Pagination = ({searchResults}: { searchResults: number}) => {
    const allSearchResults = async () => {}
    return(
        <></>
    )
}

export default Pagination;